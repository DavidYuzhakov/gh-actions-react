name: Test React App
on: push
jobs:
  test_my_app:
    runs-on: windows-latest
    steps:
      - name: Get repository code
        uses: actions/checkout@v3
      - name: Install dependecies
        run: npm ci
      - name: Test app
        run: npm run test 